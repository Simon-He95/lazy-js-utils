import { describe, expect, it } from 'vitest'
import { sliderValidation } from '../../src/canvas'

describe('sliderValidation test', () => {
  it.skip('test', () => {
    sliderValidation('../../assets/kv.png', 'body')
    expect(true).toBe(true)
  })
})
